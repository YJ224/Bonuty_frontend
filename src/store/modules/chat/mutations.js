export default {

	// chat:open
	// --------------------------------------------------
	'chat:open' (state) {
		state.open = true
	},

	// chat:close
	// --------------------------------------------------
	'chat:close' (state) {
		state.open = false
	}
}
